query GetStagingBatchById($id: UUID!) {
  stagingBatchById(id: $id) {
    id
    batchProcessorName
    supplierId
    supplierName
    startedAt
    completedAt
    status
    notes
    errorMessage
    stagingPurchaseOrders {
      id
      supplierReference
      orderDate
      status
      isImported
      errorMessage
      itemCount
    }
  }
}
